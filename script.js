// Basic interactivity (e.g., alert on button click)
document.querySelectorAll('.profile-actions button').forEach(button => {
  button.addEventListener('click', () => {
    alert('Feature under construction!');
  });
});